# Scope

OpenTelemetry traces are exported to configurable endpoint.
Prometheus metrics are exposed via /metrics endpoint.
Request IDs are propagated through traces and logs.
Tracing spans are structured (method, path, status, duration).
Metrics include request count, duration, errors, and in-flight requests.
