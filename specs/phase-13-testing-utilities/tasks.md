# Tasks

- [ ] Create `TestApp` builder with memory adapters
- [ ] Implement `MockAuth` adapter (configurable behavior)
- [ ] Create `Fixtures` module with test data generators
- [ ] Add test assertions (assert_response!, assert_error!, etc.)
- [ ] Create `TestConfig` for test-specific configuration
- [ ] Add `TestApp::request()` helper for HTTP requests
- [ ] Add `TestApp::state()` for accessing AppState
- [ ] Add testing utility tests (TestApp, MockAuth, fixtures)
- [ ] Update examples to use testing utilities
