# Scope

OpenAPI spec is generated from routes and request/response types.
Route discovery is automatic (method, path, handler).
Schema generation uses serde for JSON Schema.
Swagger UI is served via /docs endpoint.
Spec is valid OpenAPI 3.0.
