# Acceptance Criteria

1. OpenAPI spec is generated from routes
2. Request/response types generate schemas
3. Swagger UI is served via /docs
4. Spec is valid OpenAPI 3.0
5. Route discovery is automatic
6. Schema generation uses serde
7. Tests verify generation, schema, and Swagger UI
