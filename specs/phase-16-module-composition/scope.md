# Scope

Modules compose explicit state and layers (no hidden DI).
Modules provide routes, middleware, and state.
Modules are registered via builder pattern.
Modules are testable in isolation.
Built-in modules: AuthModule, StorageModule, AgentModule.
Custom modules are supported via trait implementation.
