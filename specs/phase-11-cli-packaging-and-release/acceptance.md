# Acceptance

- [ ] `arqen new demo && cd demo && cargo check` succeeds.
- [ ] CLI help matches command docs.
- [ ] A source change is observed by the documented reload loop.
- [ ] Clean Docker build starts and `/health` returns 200.
- [ ] `check` detects a deliberately broken project.
