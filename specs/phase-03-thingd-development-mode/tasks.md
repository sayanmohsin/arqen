# Tasks\n\n1. Define backend traits.\n2. Implement memory semantics.\n3. Implement HTTP translation and auth headers.\n4. Add fixture/reset helpers.\n5. Run identical contract tests against both backends.\n\n## Agent rule\n\nComplete tasks in order. If a task requires a contract change, stop, record the issue in `handoff.md`, and update the relevant interface specification before continuing.\n
