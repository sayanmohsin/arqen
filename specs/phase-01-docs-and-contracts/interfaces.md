# Interfaces\n\nConfiguration keys: `ARQEN_HOST`, `ARQEN_PORT`, `ARQEN_LOG`, `ARQEN_STORAGE_MODE`; endpoints `/health`, `/ready`, `/docs`, `/agent`; startup banner fields; tool metadata fields; job states `queued`, `leased`, `completed`, `retrying`, `dead`.\n\n## Compatibility rule\n\nKeep interfaces small, typed, documented, and replaceable. Do not expose private implementation details through public application contracts.\n
