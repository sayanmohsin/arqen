# Test plan

Unit URL/serialization/header/error mapping; contract scenarios against native
and HTTP; failure matrix for auth, conflicts, throttling, server errors, bad
JSON, timeouts, and unavailable server; security assertions for token redaction.
